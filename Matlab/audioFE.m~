function aFE = audioFE ()
    %Usada para definir los atributos que analizaremos de los audios
    fs = 44100; 
    %Se definen los valores de longitud de la trama y numero de
    %muestras solapadas 
    aFE = audioFeatureExtractor(...
    "SampleRate",fs, ...
    "Window",hamming(round(0.04*fs),"periodic"), ...
    "OverlapLength",round(0.03*fs), ...
    "mfcc",true, ...
    "melSpectrum",true, ...
    "pitch",true, ...
    "spectralCentroid",true);
end

%69.1656